table:
  name: issuing_requests_view
  schema: public
select_permissions:
  - role: manager
    permission:
      columns:
        - badge_description
        - badge_id
        - badge_title
        - badge_version
        - candidature_evidences
        - engineer_id
        - engineer_name
        - id
        - is_issued
        - manager_id
      filter: {}
      allow_aggregations: true
      query_root_fields:
        - select
      subscription_root_fields: []
